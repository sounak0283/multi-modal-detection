# Development and training dependencies - NEVER shipped.
# See NOTICE.md section 4: a training-only dependency can still taint the weights.
-r requirements.txt
pytest>=8.0
ruff>=0.6
pip-licenses>=5.0
httpx>=0.27  # FastAPI TestClient transport
playwright>=1.40  # Apache-2.0; UI end-to-end tool, dev only
requests>=2.32     # already runtime; listed for tools/ui_e2e.py
